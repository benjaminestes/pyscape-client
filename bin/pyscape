#! /usr/bin/env/ python

import sys

from pyscape import Pyscape

def main():
    keys = {
    "access_id": "test-id",
    "secret_key": "test-key",
    "level": "full"
    }
    
    print(Pyscape(**keys))

if __name__ == '__main__':
    sys.exit(main())